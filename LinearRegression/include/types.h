#ifndef TYPES_H
#define TYPES_H

#include <vector>
#include <utility>

using vp = std::vector<std::pair<float, float>>;
using pvp = std::pair<std::vector<std::pair<float, float>>, std::vector<std::pair<float, float>>>;

#endif